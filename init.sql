-- Database initialization script
-- This script runs automatically when PostgreSQL container starts for the first time

-- Create database if it doesn't exist (already created via POSTGRES_DB env var)
-- But we can add any initial setup here

-- Example: Create extensions if needed
-- CREATE EXTENSION IF NOT EXISTS "uuid-ossp";

-- Tables are created by SQLAlchemy models, but you can add seed data here if needed
-- INSERT INTO users (email, full_name, is_active) VALUES 
-- ('admin@example.com', 'Admin User', true)
-- ON CONFLICT (email) DO NOTHING;

